{
  "name": "@context-health/mcp-server",
  "version": "0.1.0",
  "description": "Local stdio MCP server exposing the Context Health Check scoring engine as tools for Claude Desktop and Claude Code.",
  "type": "module",
  "bin": {
    "context-health-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@context-health/core": "*",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  },
  "license": "MIT"
}
